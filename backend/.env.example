# Supabase — self-hosted on VPS SumoPod (43.157.247.84).
# Default below is the Kong API gateway on port 8000 (Supabase Docker default).
# In prod / from GitHub Pages, this MUST be served over HTTPS or browser
# mixed-content rules will block all requests — front a TLS reverse proxy
# (Caddy / nginx + Let's Encrypt) and use https://supabase.mshadianto.id.
SUPABASE_URL=http://43.157.247.84:8000
SUPABASE_ANON_KEY=__paste_anon_public_key_from_supabase_settings__
SUPABASE_SERVICE_KEY=__paste_service_role_key_NEVER_COMMIT_THIS__

# Groq (for Tanya HR)
GROQ_API_KEY=__paste_groq_key__

# App
ENVIRONMENT=development
JWT_SECRET=__generate_with_openssl_rand__
